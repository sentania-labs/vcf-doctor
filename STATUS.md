# STATUS

Updated by the lead agent after every integration.

## Working
- Phase 0 skeleton: contracts (`backend/app/models`), FastAPI `/api/health`,
  Vite/React/Tailwind shell served by the backend, SQLite settings KV,
  Makefile, Dockerfile, compose.

## In progress
- Phase 1 parallel build (Agents A through F). See docs/PLAN.md.

## Broken
- nothing known

## Stretch
- SDDC Manager discovery, NSX, topology, correlation, cross-vCenter diff.
